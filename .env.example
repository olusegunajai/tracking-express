# Database Configuration
# For local development, the app uses better-sqlite3.
# For production (Netlify/Neon), set these variables:
DATABASE_URL=
JWT_SECRET=your_super_secret_jwt_key
ADMIN_PASSWORD=admin123
